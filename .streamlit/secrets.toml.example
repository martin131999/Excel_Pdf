# Copy this file to secrets.toml (same folder) and fill in real values.
# secrets.toml is gitignored — never commit it.

[sqlserver]
connection_string = 'DRIVER={ODBC Driver 18 for SQL Server};SERVER=52.77.57.182;DATABASE=Test;UID=testuser;PWD=StratAgile123!;Encrypt=yes;TrustServerCertificate=yes;'
